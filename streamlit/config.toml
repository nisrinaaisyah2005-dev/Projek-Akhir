[theme]
primaryColor             = "#E6A817"   # kuning — tombol, tab aktif, slider
backgroundColor          = "#FFF8DC"   # kuning soft — latar halaman utama
secondaryBackgroundColor = "#F5D76E"   # kuning lebih tua — sidebar & card
textColor                = "#2e1f00"   # coklat tua — teks

font = "sans serif"

[server]
maxUploadSize = 500
